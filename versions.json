{
  "scheme": "V<YYYYMMDD><letters>",
  "note": "版本号由 version_bump.py 维护：同日每次推送取下一个字母；a..z, aa..zz, aaa..；跨天重置为 a。files 记录每个文件最后一次推送时的版本，index.html 恒等于全站最新版本。",
  "day": "20260924",
  "seq": 14,
  "latest": "V20260924n",
  "updatedAt": "2026-09-24T16:39:53+08:00",
  "note2": "朝暮计 v64：时间周期筛选进「清单 / 专注」各自的侧边栏。【清单页】侧边栏新增「时间周期」段（位置在「展示列表」之后、「生成」之前），8 项 = 全部 / 今日 / 明日 / 本周 / 本月 / 逾期 / 已完成清单 / 无日期，竖排条目 + 当前项高亮 + ✓，每项带实时计数（口径与主区列表同源）；原先散在「展示列表」段里的「逾期任务」「已完成清单」两条目升级进这一段（旧的 sOverdueCount / sArchivedCount 节点不再存在）；顶部那条「周期 ▾」下拉与面板一并移除（按 Tom 定：周期只从侧边栏选），标题只保留「四象限 全部·N待办」这样的只读标签，而 filter=archived 时标题右侧仍给「← 返回」显式出口。【专注页】侧边栏新增同一段（放在「操作」之前，不带计数 —— 跟随该页侧栏既有风格），段下写明口径：「只筛「待办」组 · 习惯与目标没有日期，照常显示」。★ 专注页筛选只作用于「待办」组：习惯与目标一律放行，页脚改成「筛选后」口径并写出「（周期：X）」，空态分成两种文案 —— 「周期「明日」下没有待办的专注任务」与「还没有专注任务」不再混用。★ 计时中的卡片强制保留（正在走的那张卡不会被周期筛掉），否则周期一切换，卡片连同「⏵ 回到计时页」入口会一起消失。【搜索态】周期段整体置灰并写明「搜索中 · 周期不参与筛选」，且靠选周期的函数内部守卫拦住程序化点击（仅 pointer-events 挡不住 el.click()）。★ 两页状态各自独立：清单页沿用原 filter，专注页新增 focusFilter，互不覆盖。★ 本轮修掉一处真回归：matchFilter 加第二参数后，调用点写成 `.filter(matchFilter)` —— Array.prototype.filter 会把**下标**当第二参数传进来（f 变成 0/1/2…），一路落到末尾 return true，导致周期筛选整体失效；已改为箭头调用 `.filter(t => matchFilter(t))` 并在函数首行加类型防御，闸门里直接断言 getFilteredTasks() 的返回值（不再只靠主区文本间接推断）。验收：真机（Edge headless）双向闸门 —— 打补丁前 14 PASS / 57 FAIL（剩下的 14 条恰为全部控制项），打补丁后 71 PASS / 0 FAIL。",
  "note2_prev": "朝暮计 v64：时间周期筛选进「清单 / 专注」各自的侧边栏。【清单页】侧边栏新增「时间周期」段（位置在「展示列表」之后、「生成」之前），8 项 = 全部 / 今日 / 明日 / 本周 / 本月 / 逾期 / 已完成清单 / 无日期，竖排条目 + 当前项高亮 + ✓，每项带实时计数（口径与主区列表同源）；原先散在「展示列表」段里的「逾期任务」「已完成清单」两条目升级进这一段（旧的 sOverdueCount / sArchivedCount 节点不再存在）；顶部那条「周期 ▾」下拉与面板一并移除（按 Tom 定：周期只从侧边栏选），标题只保留「四象限 全部·N待办」这样的只读标签，而 filter=archived 时标题右侧仍给「← 返回」显式出口。【专注页】侧边栏新增同一段（放在「操作」之前，不带计数 —— 跟随该页侧栏既有风格），段下写明口径：「只筛「待办」组 · 习惯与目标没有日期，照常显示」。★ 专注页筛选只作用于「待办」组：习惯与目标一律放行，页脚改成「筛选后」口径并写出「（周期：X）」，空态分成两种文案 —— 「周期「明日」下没有待办的专注任务」与「还没有专注任务」不再混用。★ 计时中的卡片强制保留（正在走的那张卡不会被周期筛掉），否则周期一切换，卡片连同「⏵ 回到计时页」入口会一起消失。【搜索态】周期段整体置灰并写明「搜索中 · 周期不参与筛选」，且靠选周期的函数内部守卫拦住程序化点击（仅 pointer-events 挡不住 el.click()）。★ 两页状态各自独立：清单页沿用原 filter，专注页新增 focusFilter，互不覆盖。★ 本轮修掉一处真回归：matchFilter 加第二参数后，调用点写成 `.filter(matchFilter)` —— Array.prototype.filter 会把**下标**当第二参数传进来（f 变成 0/1/2…），一路落到末尾 return true，导致周期筛选整体失效；已改为箭头调用 `.filter(t => matchFilter(t))` 并在函数首行加类型防御，闸门里直接断言 getFilteredTasks() 的返回值（不再只靠主区文本间接推断）。验收：真机（Edge headless）双向闸门 —— 打补丁前 14 PASS / 57 FAIL（剩下的 14 条恰为全部控制项），打补丁后 71 PASS / 0 FAIL。",
  "files": {
    "index.html": "V20260924n",
    "sync-github.js": "V20260923h",
    "tools/CPA会计掌握度系统.html": "V20260917i",
    "tools/code-previewer.html": "V20260917i",
    "tools/cpa学习工具1.html": "V20260924k",
    "tools/life-workbench.html": "V20260923g",
    "tools/人积极交往.html": "V20260918h",
    "tools/健康管理.html": "V20260917i",
    "tools/偶像学习.html": "V20260917i",
    "tools/备考学习工作台.html": "V20260917i",
    "tools/学习目标管理台.html": "V20260917i",
    "tools/工作管理系统.html": "V20260917i",
    "tools/恒星时间管理法.html": "V20260917i",
    "tools/旅行助手.html": "V20260917i",
    "tools/日记.html": "V20260918o",
    "tools/时间统计.html": "V20260917i",
    "tools/时间统计2.html": "V20260917i",
    "tools/朝暮计.html": "V20260924m",
    "tools/消息获取.html": "V20260917i",
    "tools/阅读 (2).html": "V20260917i",
    "tools/social/record.html": "V20260918h",
    "tools/social/model.html": "V20260918h",
    "tools/social/loop.html": "V20260918h",
    "tools/social/learn.html": "V20260918h",
    "tools/social/toolkit.html": "V20260918h",
    "tools/social/path.html": "V20260918h",
    "tools/social/action.html": "V20260918h",
    "tools/social/wisdom.html": "V20260918h",
    "tools/backup-hub.js": "V20260923h",
    "tools/sync-diff.html": "V20260923j"
  }
}
